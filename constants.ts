
import { IndustryBenchmarks } from './types';

export const DEFAULT_BENCHMARKS: IndustryBenchmarks = {
  clickToConvoMin: 0.20, // 20%
  clickToConvoMax: 0.40, // 40%
  convoToSaleMin: 0.05,  // 5%
  convoToSaleMax: 0.15   // 15%
};

export const CURRENCY = 'FCFA';
